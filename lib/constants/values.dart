import 'package:flutter/material.dart';

// font size
const headerText = 22.0;
const subheaderText = 17.0;
const bodyText = 15.0;

// font weight
const bold = FontWeight.w700;
const regular = FontWeight.w500;

// letter spacing
const textSpacing = 0.5;

// colors
const primaryColor = Color(0xFF2E3E5C);
const accentColor = Color(0xFF9FA5C0);
const actionColor = Color(0xFF1FCC79);
const actionAccentColor = Color(0xFFFF6464);
const buttonColor = Color(0xFFFFFFFF);

// border radius
final borderRadius = BorderRadius.circular(50);

// padding
const standardPadding = EdgeInsets.all(16.0);
const largePadding = EdgeInsets.all(32.0);
